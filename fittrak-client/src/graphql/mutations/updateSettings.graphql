mutation updateSettings($height: Float, $weight: Float, $preferredUnit: UnitEnumType!) {
  updateSettings(height: $height, weight: $weight, preferredUnit: $preferredUnit) {
    viewer {
      id
      profile {
        height
        weight
        preferredUnit
      }
    }
  }
}
