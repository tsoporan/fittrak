mutation addExercise($workoutId: Int!, $exerciseName: String!) {
  addExercise(workoutId: $workoutId, exerciseName: $exerciseName) {
    workout {
      id
      exercises {
        id
        name
      }
    }
  }
}
