mutation addExercises($workoutId: Int!, $exercises: ExercisesInputType!) {
  addExercises(workoutId: $workoutId, exercises: $exercises) {
    workout {
      id
      exercises {
        id
        name
        isActive
        dateStarted
        dateEnded
      }
    }
  }
}
