query workout($workoutId: Int!) {
  viewer {
    workout(workoutId: $workoutId) {
      id
      status
      isActive
      exercises {
        id
        name
        dateStarted
        dateEnded
        isActive
      }
    }
  }
}
