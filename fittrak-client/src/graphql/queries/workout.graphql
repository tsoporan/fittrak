query workout($workoutId: Int!) {
  workout(workoutId: $workoutId) {
    id
    status
    dateStarted
    dateEnded
    exercises {
      id
      slug
      name
      dateStarted
      dateEnded
      sets {
        id
        weight
        repetitions
        unit
      }
    }
  }
}
