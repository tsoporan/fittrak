query workout($workoutId: Int!) {
  viewer {
    workout(workoutId: $workoutId) {
      id
      status
      exercises {
        id
        name
        dateStarted
        dateEnded
        isActive
      }
    }
  }
}
