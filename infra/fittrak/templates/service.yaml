apiVersion: v1
kind: Service
metadata:
  name: {{ template "fittrak.fullname" . }}
  labels:
    app: {{ template "fittrak.name" . }}
    chart: {{ template "fittrak.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "fittrak.name" . }}
    release: {{ .Release.Name }}
